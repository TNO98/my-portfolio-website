import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const jetbrainsMono = JetBrains_Mono({
  variable: "--font-jetbrains-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Malay Sarkar - Java Full-Stack Developer",
  description: "Professional portfolio of Malay Sarkar, a skilled Java Full-Stack Developer with expertise in Spring Boot, React/Next.js, PostgreSQL, and AI integrations.",
  keywords: ["Java Developer", "Full-Stack Developer", "Spring Boot", "React", "Next.js", "PostgreSQL", "AI Integration"],
  authors: [{ name: "Malay Sarkar" }],
};

export const viewport = {
  width: 'device-width',
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} ${jetbrainsMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
